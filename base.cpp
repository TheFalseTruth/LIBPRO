#include
#include
#include

#define USER_MAX 1000

using namespace std;

struct user{
string identification_code, SSN, name, birthday, occpation, email;
};

void storeUserData(user a);

int main()
{
user nUser[USER_MAX];
return 0;
}

void storeUserData(user a){

};
